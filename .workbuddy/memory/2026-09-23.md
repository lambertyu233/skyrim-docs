# 2026-09-23

## OAR mod 目录结构（两个阶段）

### 阶段一：回答"制作 OAR mod 的目录结构应该怎样"
- 改 `oar-kb/02-structure/directory-structure.md`（v1.0.0 → 1.1.0）：加「实测：整合里真实存在的
  三种形态」节（标准原生 / CATA 条件与动画分居两个 mod / DAR 迁移残留数字目录名）。

### 阶段二：多源调研"所有可能的目录结构"
- **新建 `oar-kb/02-structure/directory-layouts-catalog.md`**（oar-kb 33 → 34 条），核心结论：
  - **源码级硬约束**：剥掉 `OpenAnimationReplacer\<Mod>\<Submod>\` **恰好三段**后，剩下的字符串
    必须逐字等于 `data\meshes\` 下的原版路径（两端均 `ToLower` 精确比较）。
    证据：`Parsing.cpp::StripReplacerPath`（注释写明 strip 两层子目录）、
    `ReplacementAnimation.cpp::GetOriginalPath()`、`OpenAnimationReplacer.cpp::CreateReplacementAnimations`、
    `Parsing.cpp::CacheDirectoriesInternal`（`data\meshes\` 递归找 `openanimationreplacer` / `dynamicanimationreplacer`）。
  - **本机 1920 mod 实测**：49 个 OAR 根 / **8 种插入点**（`animations\` 后 32、`Meshes\` 根 1、
    `character\` 后 1 = Live Action、`_1stperson\animations\` 后 2、dragon/horse 各 1、
    大写变体 11）；419 个 submod；submod 内 hkx 首段分布 `animations` 1749 / 根下 1520 /
    `Actors` 892 / `female` 242 / `male` 41。
  - `_variants_<原文件名去扩展名>` 变体目录（官方原文 + `ConvertVariantsPath` 双向印证）；
    `.mohidden` 现为**子串**匹配且跳过整棵子树；`overrideAnimationsFolder` 的基准目录是
    **submod 的父目录**（`<Mod>\`）而非 submod 内。
  - Legacy（DAR）两型：`_CustomConditions\<数字>\` + `_conditions.txt`（priority = 目录名）；
    `<插件名.esp>\<FormID 十六进制>\`（priority 恒 0，自动生成 `IsActorBase`）。
- 改 `oar-kb/09-sources/official-sources.md`（v1.0.0 → 1.1.0）：写明 **2026-09-23 复核"没有官方 wiki"**
  的三条依据，并补源码定位（`Parsing.cpp` / `ReplacementAnimation.cpp` / `OpenAnimationReplacer.cpp`）。
- 改 `01-navigation/troubleshooting-index.md`（v1.1.0 → 1.2.0）：加症状行
  「自己打包的 OAR mod 整个不生效」→ 分叉点 = 插入点与 submod 内路径起点不匹配。
- 自检：oar-kb `build_index` 34 条 / validate 0 ERROR / check_links 213 条全有效 / check_index_ui 24 项全过；
  01-navigation validate 0 ERROR。

### 环境变化（已写入 MEMORY.md）
- **活动实例 = `D:\game\PureLOTD`**（1920 mods）。`D:\game\JIZIYU J5.0`、
  `E:\game\JIZIYU Y5.0` 均不存在，`E:`/`F:` 盘不可访问；obito 拉弓 mod 两条旧路径同时失效。

### 命令坑
- 本机 **无 `openpyxl`** → 读 `.xlsx` 直接用 `zipfile` + `xml.etree` 解
  `xl/sharedStrings.xml` 与 `xl/worksheets/sheetN.xml`（脚本留在 `.workbuddy/tmp/read_la_xlsx.py`）。
  同一目录另有 `scan_oar_struct.py` / `scan_oar_samples.py`（OAR 目录形态统计，可复用）。
- 遍历 `C:\Users` 找日志会被沙箱拦（命中 `.ssh`）→ 直接按已知路径找，别全盘 find。

### 待办 / 遗留
- `01-navigation/troubleshooting-index.md` 通篇**反引号纯文本**引用条目路径 →
  `check_links.py` 对它等于**空跑假通过**（报 0 条）。若要真正的链接校验，
  需用技能里的 `scripts/linkify_refs.py` 做一次转换（跨库路径要人工核对）。
