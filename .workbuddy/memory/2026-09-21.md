# 2026-09-21 工作日志

## 新建资料库：`behaviour-engine-kb/`（FNIS / Nemesis / Pandora 动作引擎）

- 触发：用户给出一份"行为引擎知识清单"，要求补充搜索后整理成可维护知识库（用 `build-maintainable-kb` 技能）。
- 产出：28 条目 / 8 分类的 Markdown 资料库，含 `index.json` + 离线浏览器 `index.html`（两栏布局 + 内联渲染正文 + 分类过滤 + 检索）。
  - 分类：`00-overview`(3) `01-principles`(5) `02-fnis`(3) `03-nemesis`(3) `04-pandora`(5) `05-ecosystem`(3) `06-practices`(3) `07-sources`(3)
- 关键一手来源（已存 `_raw/`）：
  - Pandora 官方 Wiki 首页（**Havok Behavior = 非确定性有限状态机中间件，序列化进 hkx 包**；**Behaviour Patcher 定义**；**三引擎对比表**）
  - Pandora 仓库 README（**"parses changes to nodes in xml, serializes them into the FSMs"**；启动参数；作者向补丁格式 xml）
  - Pandora Wiki `Performance Notes`（预加载 / 浅映射 / **增量(反)序列化** / 不用 hkxcmd / 克制并行）
  - **fore 2012 年一手帖**（`forums.nexusmods.com/topic/747532-...`）：behavior 目录下的 hkx **不是动画文件**；**hkx 只是包格式，连骨骼也压在里面** —— "hkx 有两种"的源头
  - **scorrp10**（Nexus 论坛 13497416）：动画数据库/事件名机制；**Patcher 与 Replacer 是"完全不同的物种"**
- 版本事实（已核对官方页）：FNIS **7.6 / 2020-02-21 停更 / 闭源**；Nemesis 2019 起 / GPLv3 / **中级以上无公开文档**；Pandora Nexus **v4.4.0-beta（2026-08-12）** / GPLv3 / 全生物支持。
- 新增可复用脚本：`scripts/validate_kb.py`（机械校验 id==文件名、category==目录名、必填字段、index 完整性、占位符残留），构建后 `exit 0` 表示通过。

## 可复用经验

- `build-maintainable-kb` 技能的 `build_index.py` 从 **`manifest.json` 的嵌套 `kb.name`** 读标题；写成扁平 `name` 会静默退化成默认标题"资料库"，不报错 —— 建完库必须 grep `<title>` 复核。
- 技能自带的 `build_index.py`（新版）与 `community-shaders-kb/scripts/build_index.py`（旧版）**不是同一份**：旧版把分类标签硬编码在脚本里，新版从 manifest 的 `categories[].dir/title` 读。**新建库用技能里的那份**。
- 本机 Bash 的 PATH 依旧损坏，脚本前需 `export PATH="/usr/bin:/bin:$PATH"`；建库流程本身可全程用 `Write` 工具 + 托管 Python 完成。

## 修复 `behaviour-engine-kb/index.html` 的交互问题（1.0.1）

- **修的 bug**：分类过滤点不回「全部」。模板里 `全部` 按钮是写死的 HTML，但事件只在 `renderNav()` 里绑给了 `#catnav` 内的分类按钮 → 选中任一分类后无法回到全部。改法：把「全部」+ 分类按钮一起放进 `#navbox`，统一绑 `#navbox .navbtn`，并抽出 `selectCat(cat, btn)`。
- **新增**：分类过滤收起/展开（`#navtoggle` + `#navbox.hidden` + `aside.collapsed` 收窄侧栏，状态存 `localStorage['kb-nav-collapsed']` 并开机恢复）。
- **删除**：详情页底部 tip 里的「正文已渲染为排版好的文章（…）」冗余说明，只留源文件路径。
- 改动落在 `scripts/build_index.py` 的 `HTML_TEMPLATE`，重新生成后 `validate_kb.py` **0 错误**。
- 版本记录已写入该库 `CHANGELOG.md`（1.0.1）。

### 可复用经验（重要）

- **同批并发编辑同一文件会丢改动**：一次消息里发两个 Edit 打同一个文件，第二个可能被第一个的写入覆盖（本次就这样丢过一次）。**同一文件的多处修改必须串行**。
- **改 HTML 生成器的正确姿势**：改 `build_index.py` 的模板 → 重新跑 `build_index.py` → 再 grep 生成物复核。**不要直接手改 `index.html`**，下次重建就没了。
- **验证生成页面的交互功能，不必装 Chromium**（agent-browser 要下 ~500MB，本机未安装、不值得）：写个一次性 `_make_domtest.py`，把 `index.html` 里的 `<script>` 抽出来，前面套一层最小 DOM 桩（`El` 类实现 `classList/innerHTML/querySelectorAll/dataset`，`document.getElementById/querySelector(All)` 只支持用到的几个选择器，`localStorage` 用内存对象），用正则把巨大的 `const ENTRIES = [...]` 换成 3 条假数据，再在尾部追加断言跑 `node`。本次 23 项断言全过，其中「『全部』按钮已绑定点击事件」这条正是能在旧代码上失败的回归项。用完删掉临时文件。
- **同类 bug 的波及范围**：`community-shaders-kb`、`creation-kit-kb`、`mo2-usvfs-kb` 的 `build_index.py` 有**完全相同的未绑定 bug**（前两个还与技能原版各有 900+ 行差异）。

## 修技能 + 四个库统一重跑网页（承接上一条，用户改口要求全改）

用户先选「只修这一个新的」，随后改口：「另外三个资料库也一并改了吧，同时把技能也要修改下，以防再出现这种情况」→ 最终又追加「先修改好技能，然后再把其他几个知识库按照新技能重新跑下生成网页部分」。

### 技能改动（`~/.workbuddy/skills/build-maintainable-kb`，`agent_created: true`）
- `scripts/build_index.py` 模板修好：`#navbox` 内「全部」+ 分类按钮由 `document.querySelectorAll('#navbox .navbtn[data-cat]')` **一行统一绑定**；新增 `selectCat` / `setNavCollapsed` / `toggleNav`（`aside.collapsed` 收窄 78px、`localStorage['kb-nav-collapsed']` 记忆）；删掉详情页冗余 tip。
- **新装两个脚本**：`scripts/validate_kb.py`（结构+索引+模板校验，兼容旧 `index.json` 结构，不误报）、`scripts/check_index_ui.py`（索引页交互回归：抽内联 `<script>` + DOM 桩，在 Node 里真跑页面脚本并模拟点击）。
- `SKILL.md` 打了 10 处补丁（103 → 131 行）：产出结构补两个脚本、新增「侧栏筛选按钮绑定铁律」小节、两个脚本的用法说明、工作流第 5 步改为「build → validate → check_ui 两个都过才算完成」、环境坑里补上实测的 Edit 并发失败姿势与「先全部校验再统一写盘」的脚本替换纪律、注意事项加「改模板等于改产品」。

### 四个库
- 用**同一个补丁脚本**给 4 份 `build_index.py` 打同一套 UI 补丁（先断言每处锚点恰好命中 1 次，不中就整体中止、不写任何文件）。
- `mo2-usvfs-kb/scripts/build_index.py` 现在与技能那份**逐字节相同**；`behaviour-engine-kb` 手工改的 CSS 顺序/`80px`/`#navbox .navbtn` 也归一到了规范版。四库 + 技能的 UI 区块指纹已全部一致。
- `creation-kit-kb`、`community-shaders-kb` **保留自己的脚本**（前者的 status 徽章/状态过滤、后者的 `CATS` 数组 + `category_label`/`path` + 不内联正文的 `index.json` 是有效功能/旧结构），只把 UI 区块对齐——**不要用技能 stock 脚本整体覆盖它们**。
- 四库全部重建 + 双校验通过：validate 0 错误、check_index_ui 20/20。三库 CHANGELOG 补了 1.0.1（mo2 是 1.1.1）。

### 可复用经验（更新上面那条）
- DOM 桩测试已产品化：以后直接用 `scripts/check_index_ui.py`，不用再临时写。它用**页面真实的 ENTRIES**（不替换假数据），所以能顺带验证真实数据下的分类计数。
- 写 DOM 桩的一个坑：`innerHTML` 重写时**必须把旧子元素从全局注册表摘掉**，否则条目计数会累加、测试假失败（本次先踩到 28→33→61 的累加）。
- 用「区块指纹」（正则截取 UI 区块算 md5 对比）可以低成本证明多份衍生脚本的模板真的同源，比逐行 diff 可读。
- `cp` 类操作要注意**顺序**：本次把校验脚本复制到其他库时还没改写它，导致三个库跑的是旧严格版、误报 49 条；改写后必须重新同步。

## community-shaders-kb 对齐新版模板的差异评估（用户要求「先告诉我」，**未动手**）

摸清了它到底差在哪。结论：**frontmatter 早就一致，差的是中间层**。
- **已一致（不用动）**：8 个必填 frontmatter 字段（额外多 `kind`/`status`）；`00-overview…05-tools` 的 `NN-` 目录结构，`02-features/core|additional` 子目录新版脚本兼容（按**顶层**目录定 category）；侧栏 UI 区块（上一轮已对齐）；`validate_kb.py` + `check_index_ui.py` 已同步。
- **需改 ①`manifest.json`**：现为扁平 `name`/`description` + `categories[{key,label}]`；新版读 `kb.name`/`kb.description` + `categories[{dir,title}]`。不改的话页头标题会退回默认「资料库」（页头标题目前在模板里硬编码）。
- **需改 ②`index.json` 数据层**：现为 `{meta:{generated_at,kb_version,total,by_category}, entries}` 且 `by_category` 键用**中文标签**；新版是扁平 `{generated,total,by_category,entries}` 且键用**目录名**，entries 内联 `content` 并带 `_file`/`_chars`（现版刻意剔掉 content，还有 `category_label`/`path` 两个新版没有的字段）。内联后 index.json 会从 34KB 涨到约 350KB。
- **需改 ③页面模板绑定**：`CATS` 现为 `[{key,label}]` 数组（新版是 `{dir:count}` 对象 + `CAT_LABELS`）；`renderNav/renderStats` 不带条目计数（新版有 `.cnt`/`#allcnt`）；`showDetail` 用 `e.path`/`e.category_label`（新版 `e._file`/`label(e.category)`）；详情页 kind 芯片样式、`源文件：` 的 `.tip` 类、页面缺 `.cnt`/`.tip`/`.chip.muted` 三处 CSS。
- **发现的小 bug**：脚本里 `meta.generated_at` 硬编码 `"2026-09-20"`、`kb_version` 硬编码 `"1.0.0"`，**重建也不会更新**（该库没 `import datetime`）。
- **必须移植保留（新版没有，覆盖就会静默丢掉）**：`statusBadge()` + `kind`(core/additional)/`status`(released/TBA) 徽章与 `.b-core/.b-add/.b-tba` CSS；侧栏「状态过滤」三按钮及 `curStatus` 筛选；`scripts/gen_features.py`（47 个功能条目由它生成）。
- **纪律**：对齐要用「新版模板为基线 + 增量回灌上面四项」的方式重写 `build_index.py`，**不能拿技能 stock 脚本整体覆盖**。改完须过 validate（0 错误且不再打印那两条 NOTE）+ check_index_ui（20/20），并自行给 status 过滤补断言。
- 该库 `build_index.py` 538 行，与新版差异 299 行（含整段 HTML 模板）。

## community-shaders-kb 正式对齐（用户：「丢掉就行了，跟新版结构对齐，只要不改我的知识内容」）

按用户决定，上面的「必须移植保留」清单**全部放弃**：status 徽章/状态过滤/`CATS` 数组/`category_label`+`path` 一律移除，`gen_features.py` 保留但属遗留工具。**知识内容（标题/正文/摘要/标签/来源）一字未改。**

### 关键发现（原评估漏掉的坑）
- 查出 **`02-features` 下 47 个条目的 frontmatter 写的是 `category: features`**，与目录名 `02-features` 不符。旧脚本用目录名兜底所以没暴露；stock 脚本按 frontmatter 取值 → 查不到中文标签 → 页面分类标签变空。**我在给用户的第一版评估里说「frontmatter 已一致」，这是错的**，实际那 47 个文件并不一致。
- **CRLF 陷阱**：那 47 个功能条目是 CRLF，其余 15 个是 LF。改写 frontmatter 必须按字节处理并保留原换行符（按行 split/join，不用文本模式 `w`）。
- 改写方法：逐文件断言「`category: features` 恰好命中 1 次」→ 只替换那一行 → **逐行比对证明只有该行不同、行数不变、字节增量等于替换差** → 全部通过才统一写盘。47 个文件，复核异常 0 处。

### 落地动作
- `scripts/build_index.py`：直接换成技能 stock 版（**diff 确认与技能逐字节相同**）。
- `manifest.json`：`name`/`description` → 嵌套 `kb`；`categories[].key/label` → `dir/title`；补 `tooling`。踩到一次：`source_name` 与 description 开头重复，导致副标题里「基于 modding.wiki 官方文档」出现两遍 → 删掉 `source_name`（description 自带出处时就不该再填）。
- `index.json`：34KB → **112.5KB**（内联正文），顶层扁平、`by_category` 键改目录名、日期改为构建当天（原来硬编码 `2026-09-20`，重建不动）。
- 备份：`_raw/legacy-community-2026-09-21/`（该库无 git）。

### 防复发（技能侧，这才是重点）
- `validate_kb.py`：把 `category_matches` 的**「允许省略 `NN-` 前缀」宽容取消，改为严格相等** —— 正是这条宽容放过了那 47 个真实错误。另新增**「每个有条目的目录必须在 `manifest.categories[].dir` 里声明过」**的交叉校验。同步到 4 个库。
- 两处都做了**反向验证**：故意改回 `category: features` → 报错退出 1；manifest 删掉一个目录声明 → 报错退出 1；测试后逐字节还原。
- `SKILL.md` 同步：`category` 一段改成「必须严格等于目录名，写短名会静默变空标签」，删掉原来那句「validate 兼容省略前缀的老写法」，validate 小节补上新检查项与本次教训（**宽容校验比没有校验更危险**）。
- 教训：**「容忍老写法」的校验逻辑是有毒的** —— 它把真实错误伪装成合规。

### 最终状态
- community-shaders-kb：validate 0 错误（原来的 2 条旧结构 NOTE 消失）、check_index_ui 20/20；`build_index.py` 与 `validate_kb.py` 均与技能逐字节相同。
- 四库全跑一遍：全部 0 错误 + 20/20。
- 版本记为 **2.0.0**（结构破坏性变更）；README/CONTRIBUTING 里「状态过滤」表述与脚本清单同步更新。
- 注意：**creation-kit-kb 仍保留自己的 status 徽章与状态过滤分支**（脚本没换），它的 UI 区块当初只对齐过一部分。

## creation-kit-kb 也对齐 stock 版（用户：「creation-kit-kb也一样改了」）—— 四库全统一

与 community-shaders-kb 同一套流程，但**这次没有数据要修**。

### 先摸底的结论
- `category` 与目录名**严格一致，0 处不符**（不像 community 有 47 处）→ **md 正文与 frontmatter 一个字都没动**。
- `index.json` 早已是新形状（扁平顶层 + `by_category` 用目录名 + 内联 `content` + `_file`/`_chars`），`manifest.json` 也已是 `kb` + `categories[dir,title]` —— 所以这次只缺「丢掉私有功能 + 换脚本 + manifest 补字段」。
- 差异 68 行，全是私有功能：`statusBadge()` + `.badge/.b-stable/.b-review/.b-draft` CSS、侧栏「状态过滤」4 按钮 + `curStatus` + `[data-status]` 绑定、硬编码页头标题/副标题（`<title>` 与 `<h1>/<p>` 写死，改 manifest 不生效）、`.detail .sum` 死样式。
- **换 stock 后额外获得一个改善**：本库 29 个条目正文**全部以 `# H1` 开头**，stock 的 `_strip_leading_h1()` 会剥掉它（详情面板已单独显示标题），此前是标题重复。

### 落地
- 备份 `_raw/legacy-creation-kit-2026-09-21/`（旧脚本 + 旧 manifest）。
- `scripts/build_index.py` 换成技能 stock 版（diff 逐字节相同）。
- `manifest.json` 转 stock 形状：`kb.{name,description,locale}`（**不填 `source_name`** —— description 自带出处，填了就会重复，这是 community 那次踩过的坑）、`entry_schema`→`schema`、`categories[].description`→`desc`、补 `version(2.0.0)/generated_at/maintainers/sources/license_note/index_files/tooling`。`sources` 里的 URL 是**从 29 个条目的 `source` 字段聚合出的真实地址**，没有编造。
- README/CONTRIBUTING/CHANGELOG 同步：去掉「状态过滤」表述、脚本清单补两个校验脚本、frontmatter 示例把 `status`/`kind` 标注为「保留字段（页面已不呈现徽章）」、`entry_schema` 引用改 `schema`、CHANGELOG 记 **2.0.0**。

### 顺手收尾
- `behaviour-engine-kb` 的 `build_index.py` 与技能还差 16 行（`renderNav()` 函数定义**位置**不同 + 多一个 `type="button"`，功能等价但结构分叉）→ 一并换成 stock 版，其 CHANGELOG 记 **1.0.2**。
- **至此四库三件套（`build_index.py` / `validate_kb.py` / `check_index_ui.py`）全部与技能 stock 版逐字节相同，再无分叉。**

### 验证
- creation-kit-kb：validate 0 错误、check_index_ui 20/20；页头副标题正确不重复；7 个分类 `CAT_LABELS` 全部解析成功；`statusBadge/curStatus/data-status/.b-*` 残留全为 False（index.html 里只剩脚本注释中一处「状态过滤」字样，是 stock 模板自带说明，非功能残留）。
- 四库重建全跑：全部 0 错误 + 20/20。
- 上面那条「creation-kit-kb 保留自己的脚本、不要整体覆盖」**已作废**：用户后来改主意要求全部对齐 stock 版，现已执行完毕。



## community-shaders-kb 内容增补 → 2.1.0（用户：「查找官方wiki、官方仓库readme、Nexus发布页说明、用户反馈回帖、可信网络资讯，不要CSDN/toolify一类内容农场」）

### 调研拿到的上游硬事实（可复用）
- **CS 版本现状**：稳定版 1.8.x（最新补丁 **1.8.4 = 2026-08-26**）；开发版 **1.9.0**（PR 构建，2026-09-20 的 PR #2723）。Nexus 是**唯一受支持**的玩家渠道；GitHub 上的 `1.9.0-prXXXX` 不是「最新版」。
- **受支持游戏版本**：1.6.1170(Steam) / 1.6.1179(GOG) / 1.5.97。**1.6.640、1.7.99、1.7.104 都不支持**；除 1.5.97 外无 LTS。GPU 需求官方现行口径是 **Vulkan 1.4+**（Nexus 页另写 DX11.1+，属旧表述）。
- **VR 已停止支持** → 社区分支 **Open Shaders**（Nexus 180419）。**Linux 不官方支持** → Fluorine / NaF 指南（SulfurNitride）。
- **官方渠道**：Nexus 86492、modding.wiki（用户 wiki）、**GitHub Developer Wiki = `github.com/community-shaders/skyrim-community-shaders/wiki`（进阶信息，本库此前完全没引用过）**、Discord `nkrQybAsyy`。仓库有旧地址 `doodlum/skyrim-community-shaders` 与新地址 `community-shaders/...`，同一项目。
- **Developer Wiki 里有一张官方「CS / ENB / CS VR / ENB VR 全功能对照矩阵」（约 60 项）** + **A/B 测试（Test Interval → CommunityShadersTEST.json）** + **`TESTCUBEMAP` / `LLFDEBUG` Shader Defines 调试开关** + 官方「潜在功能依赖图」（Deferred 与 TOD/Weather 是两个巨型枢纽）。这些用户 wiki 上都没有。
- **Upscaling 不支持 XeSS**（作者因 Intel 拒绝提供支持而放弃）；**帧生成只在刷新率 ≥120Hz 生效**，需 Windowed/Borderless，建议配 SSE Display Tweaks。DLSS 版本两处官方页面不一致（Nexus 写 DLSS 4、用户 wiki 写 DLSS 4.5）。
- **功能纳入核心时间线**：1.0+ / **1.4+**（LLF、Frame Gen）/ **1.4.7+**（Terrain Shadows、ISL、Water Effects、Interior Sun、Ext. Translucency）/ **1.5.0+**（SSS 系列、Grass Collision/Lighting、ScreenSpace Shadows）/ **1.7.0+**（Sky Sync）/ **1.8.0+**（Cloud Shadows）。升级 CS 必须删对应旧附加 MOD，否则编译失败。
- 官方**推荐 MOD 清单**（Vanilla Setup Guide）：照明 True Light / Lux CS（先装 Light Placer + CS Light）、天气 NAT.CS III / Obsidian CS / Azurite III CS 等、Window Shadows Ultimate、CS Particle Patch、DIAL、Seamless Dynamic Cubemaps、PBR 包（Faultier's / Skyland / Cathedral / Vanaheimer）、Asset Doctor。互斥规则：**一室内+一室外照明、一天气；不要 EVLaS（Sky Sync 已取代）；MLO2 >1.3.6 不兼容**。

### 落地
- 新增分类 **`06-community`（社区）** + 5 个新条目：`00-overview/feature-matrix.md`、`00-overview/version-and-support.md`、`04-development/testing-and-debugging.md`、`06-community/common-pitfalls.md`、`06-community/community-resources.md`（后者含**识别内容农场/AI 聚合站**的判据）。
- **精修 15 个功能条目**（13 核心 + skylighting/upscaling）：补工作原理/算法、游戏内参数与调试开关、需求与前置、兼容性注意、贡献者。**其余 32 个仍是摘要级**（官方对它们只给了一句话，下一轮应从 Nexus 各附加 MOD 页取材，别硬凑）。
- `01-installation/vanilla-setup.md` 升 **2.0.0**，落地官方推荐 MOD 清单；`requirements.md` / `installation-guide.md` / `incompatible-mods.md` / `faq.md` / `what-is-cs.md` 同步更新。
- **新增两个脚本**：`scripts/check_links.py`（扫站内相对链接，报失效）、`scripts/fix_links.py`（按目标尾路径在根目录反查实际文件，反算正确相对路径）。**只有 community-shaders-kb 有，其余三库还没有** → 值得推广。
- **`gen_features.py` 降级为脚手架**：默认**跳过已存在文件**，需 `--force` 才重写（否则会把 15 个精修条目打回模板）。已在脚本头写了醒目警告。
- 修正 **32 处坏链**（14 处是本次新条目引入的 `02-features/*/*` 深层链接少一层 `../`，18 处是历史遗留：enb-migration / installation-guide / faq / contributing）。**全部 md 换行统一为 LF**（35 个文件，此前 LF/CRLF 混用）。
- 验证：validate 0 错误 / check_index_ui 20/20 / check_links 132 条全有效 / gen_features 跳过 47、新建 0。

### 环境与工具教训（跨项目通用，重要）
- **同一文件绝不要在同一条消息里并行发多个 Edit**：本次给 README 一次发 4 个 Edit，**只有 2 个真正落盘**（后写的覆盖先写的），而 4 个**全部返回 success**。改同一文件必须串行发；批量改动不同文件可以并行。事后用 grep 抽查关键标记才发现的。
- **目录深度决定 `../` 层数，手写极易错**：`02-features/core/x.md` 引根目录要 `../../`，`01-installation/x.md` 只要 `../`。这类错误 validate_kb.py 完全不管（它只查 frontmatter 与索引），能长期潜伏。**改动目录结构或批量写条目后，务必跑 check_links.py。**

## 第二阶段收尾：两个新脚本推广到另外三库 + 链接修复 + 换行归一（用户：「第二阶段的改动（技能 stock 版 + 另外三个库的脚本与链接修复）改吧」）

把 2.1.0 里为 community-shaders-kb 新写的 `check_links.py` / `fix_links.py` 与「修链 + 统一换行」落到另外三个库，并把账目补进各库文档。

### 关键技巧：用「修改前的 index.json」反推历史改动（本库无 git）
`fix_links.py` 不留日志、`_raw/` 也没有修复前快照。但三库的 `index.json`（**内联了每条渲染后的 HTML 正文**）mtime 为 15:09–15:10，**早于** fix_links 的 15:38 → 它就是天然的「修改前快照」。
做法：按 `_file` 把快照里的 `href="..."` 与当前 md 的 `](...)` 求差集，差集即被修的链接。结果（**我此前口头报的「creation-kit 31 / mo2 31」是错的，29 才是实数**）：
- behaviour-engine-kb **1 文件 / 1 处**；creation-kit-kb **15 文件 / 29 处**；mo2-usvfs-kb **19 文件 / 31 处**。合计 **61 处**。
- mtime 佐证：三库 15:38 被改动的 md 文件数 1 / 15 / 19，与差集算出的文件数完全吻合。
- **61 处全是同一类错误**：引用**同级或下级分类目录**的条目时漏写 `../`（正确写法几乎总是 `../0X-分类/xxx.md`）。validate_kb.py 不查正文链接，所以能长期潜伏。

### 落地
- **脚本**：三库 `scripts/` 各补 `check_links.py` / `fix_links.py`；连技能共 20 份副本，**5 个脚本每个的指纹数均为 1**（逐字节同源）。
- **换行归一**：creation-kit 4 文件（185 处）+ mo2 22 文件（714 处）CRLF → LF，共 **26 文件 / 899 处**。按字节处理 + 三重校验（行数不变、字节减少量 == CRLF 数、去 `\r` 后文本一致）。第一次跑因我自己把比较写反（拿带 `\r\n` 的原文比已去 `\r` 的新文），**26 个文件全部被守卫拦下、一个都没写**——fail-safe 生效。
- **修掉根因**：creation-kit 那 4 个 CRLF 文件是 `gen_features.py` 用文本模式 `open(p,"w",encoding="utf-8")` 写出来的。已改为 `newline="\n"`。
- **文档登记**（各记一个 minor）：behaviour **1.1.0**、creation-kit **2.1.0**、mo2 **1.2.0**；更新 CHANGELOG / README（scripts 目录树 + 维护流程）/ CONTRIBUTING（校验由两项改三项）/ manifest（tooling + version）。
- **顺手查出的登记错误**：behaviour 的 manifest `tooling` 漏登记 `check_index_ui.py`、`version` 停在 1.0.0（CHANGELOG 已 1.0.2）；mo2 的 `tooling` **只登记了 `build_index.py` 一项，而 `scripts/` 下实有 6 个脚本**、`version` 停在 1.1.0（CHANGELOG 已 1.1.1）。均已修正。
- **验证（四库全绿）**：validate 0 错误 / check_index_ui 20/20 / check_links 全有效且换行全 LF。链接数：behaviour 97、creation-kit 70、mo2 144、community-shaders 132。

### 技能侧
- `SKILL.md` 补一条：**生成器脚本自身必须写 `newline="\n"`**（默认文本模式在 Windows 上静默转 CRLF）。

### 未做（明确留给用户决定）
- community-shaders-kb 的 `gen_features.py` 同样是文本模式写文件 + 仍生成 `category: features`（2.0.0 修掉的老坑）——本次不在「另外三个库」范围内，**未动**。
- community-shaders-kb / CHANGELOG.md 第 30 行「14 个核心特性条目 + 2 个附加」与实际的 13+2=15 不符（笔误），**未动**。


---

## 16:00–17:00 新建第五个资料库：oar-kb（OAR 资料库）

**产物**：`oar-kb/` —— 10 分类 / 33 条目 / 33 个 frontmatter 条目，索引页 `index.html` 可离线浏览。
- 目录：`00-overview`(4) `01-installation`(3) `02-structure`(4) `03-conditions`(4) `04-functions`(1) `05-editor`(3) `06-plugins`(4) `07-migration`(2) `08-practices`(5) `09-sources`(3)。
- 脚本五件套 + `fetch_mediawiki.py` 从技能 stock 版复制，已校验：五库 20 份副本 + 技能 6 份，**五个核心脚本各只有 1 个指纹**（逐字节同源）。
- 本库自有的 `scripts/fetch_sources.py`：抓 GitHub raw / Nexus 网页正文到 `_raw/` 的通用抓取器（浏览器 UA）。
- 三项校验全过：`validate_kb.py` 0 错误；`check_index_ui.py` 20/20 PASS；`check_links.py` 182 条链接全部有效、全部 LF。

**关键发现（本轮新核实的事实，值得长期记住）**
1. **OAR 确实没有 wiki**：`ersh1/OpenAnimationReplacer` 仓库无 `docs/`、无 GitHub Wiki；README 是**面向编译者**的。官方"手册"= Nexus 描述页；事实终极出处 = 源码。
2. **用源码核对了条件清单完整性**：`src/Conditions.h` 里 `GetName() const override { return "..." }` 正则抽取得到 **125 个注册名**（含 `! INVALID !`/`! DEPRECATED !` 与 OR/AND/XOR/TARGET/PLAYER/MOUNT/PRESET 容器），与 Nexus 描述页清单逐条比对**无遗漏**。
3. **`IsPlayer` 条件不存在（重要勘误）**：OAR 本体 125 个名字里没有；**Detection Plugin 源码也只注册 5 个条件**（DETECTED_BY / DETECTS / DetectionDistance / DetectionRelationship / DetectionAngle）。但 Detection Plugin 页面示例写着 `DETECTS → IsPlayer`，且 LoversLab 用户也照抄——**是文档笔误**。判"是玩家"要用 `IsForm`+Player(`Skyrim.esm`/`0x7`) 或 `IsActorBase`；判"对玩家求值"用容器 `PLAYER`。
4. **OAR 插件 API 形状（源码级）**：`OAR_API::{Animations,Conditions,Functions,UI}`，头文件形式"copy into your own project"；`GetAPI(InterfaceVersion)` + 全局 `g_oar*Interface`。Conditions 的 `InterfaceVersion` 为 `V1`(已不支持)/`V2`/`V3`(Latest)，Functions 为 `V1`/`V2`(Latest)；`APIResult{OK,AlreadyRegistered,Invalid,Failed}`。**`AddCustomCondition<T>()` 必须在 `kMessage_PostLoad` 或更早调用**，晚了**静默无效**。
5. **CSDN 编造清单已逐条取证**（写进 `09-sources/unreliable-sources.md` + `_raw/misc-web.txt`）：`Data\OAR\`、`Data/OAR.json`（含 `"animations"` 数组 / `"type":"RunForward"`）、控制台 `oar list` / `oar log`、日志串 `Matched RunForward for Player with priority 100`、`.kf`/`.nif`/`.fbx` 当动画文件、`Ctrl+Shift+R` 热刷新、`OAR v5.0`、"2019 年测试 OAR v1.3"、"State Override"、把 OAR 说成操作 Animation Graph 节点树 / 插入 Blend Space·State Machine。**全部不实且可源码反驳**。
6. 社区一手补充（官方描述没写）：**预览按钮必须先选中求值目标（`prid 14`）**；**子条件必须挂在容器（OR）下面而不是挂在叶子条件上**（LoversLab，含真实 `config.json` 样本，字段有 `priority`/`replaceOnLoop`/`negated`/`editorID`）。

**来源存档**：`oar-kb/_raw/` 共 10 组文件（gh-readme、src 头文件与 API 头文件、nexus-oar-description、nexus-plugin-pages、patreon-ershin-posts、nexus-forum-threads、bahamut-tutorials、misc-web）。

**踩到的坑**：`bakemono.app`（Patreon 镜像）对 urllib 返回 502，但**搜索引擎已索引全文**，用 WebSearch 拿到的摘录反而完整；Nexus 对 urllib 返回 403 Cloudflare，但 **WebFetch 能正常抽取正文**——所以本库的 Nexus 正文靠 WebFetch，GitHub 靠 urllib。

---

## 新增：工作区知识库的「agent 入口层」

**问题诊断**（此前一直缺）：根目录没有 `AGENTS.md`，也没有任何检索脚本 →
agent 只能靠 `MEMORY.md` 里的散文描述猜到这些库存在，**未索引的知识等于不存在**。
更糟的是 `index.json` 内联了每条正文的渲染 HTML（oar-kb 单库 **296KB**），
agent 若"想先了解全貌"去读它，等于一次性吃掉整个库的上下文预算。

**实测数据**：5 库 / **197 个规范条目**（有 frontmatter 的才计数，含 CONTRIBUTING、
SOURCES.md 等不计）/ 214 个 md 文件 / 正文合计 **707KB**；
各库 index.json 92K(ck)–296K(oar)、index.html 100K–312K。

**本次新建两个文件**：

1. `scripts/kb.py`（约 380 行，零依赖单文件）——知识库检索入口：
   `list` / `toc <kb>` / `find <kw>` / `grep <正则>` / `show <id>` / `read <id>` / `check`，
   全部支持 `--json`。数据源**永远是各条目 frontmatter**，不新增需要同步的清单文件。
   - 检索打分可解释：id 精确 100 > tag 精确 40 > tag 模糊 22 > title 20 > summary 8 > 正文 3×出现次数；
   - `show` 返回**元数据 + 标题大纲**而非正文，让 agent 自己决定读不读、读哪一段；
   - `check` 校验 `AGENTS.md` 是否覆盖了所有库目录（**新增库后漏写 AGENTS.md 会报错**）。
   - 效果：`find` 返回 **1.7KB** vs 读 index.json 296KB，约 **174× 降幅**。
2. `AGENTS.md`（根目录）——agent 入口契约：四步检索协议 / 主题→库路由表 /
   证据纪律（引用落到条目路径、区分一手源-社区经验-本机实测、版本时效警示）/
   写回规则（正文进库、临时结论进日志、不变量进 MEMORY.md）/ 维护自检。

**设计取舍**：**不做**每库一张手写路由卡 `KB.md`。路由信息集中在 `AGENTS.md` 一处，
每库详情由 `kb.py toc` 从 frontmatter **按需生成**——避免又造出 5 个需要手工同步、
迟早过期的文件（符合本工作区"不新增手工同步面"的一贯取向）。

**踩坑**：本机 Bash 对中文 heredoc/输出有编码问题，写记忆用托管 Python + `newline="\n"` 更稳。

---

## 第二批：别名检索 / 排错索引 / 入口层下沉技能

**技能位置变更**：`build-maintainable-kb` 已从用户级移到
`D://game//上古卷轴5//.workbuddy//skills//build-maintainable-kb`（项目级）。原用户级路径已不存在。

### 1) aliases（检索别名）——打通的完整链路
- **schema**：新增可选字段 `aliases`（数组，≤16 项）。`tags` 回答"这篇属于什么主题"，
  `aliases` 回答"别人会用什么词来找它"。
- **build_index.py**：支持折行数组解析（`tags: [a, b,` 跨行）；解析 `aliases` 进 `index.json`；
  搜索 haystack 加入别名；新增 `showWhy()` 在搜索时于卡片标注命中原因
  （别名/标签/标题/摘要/正文），详情页把别名以弱化 chip 单列。
- **kb.py**：加权改为 id 100 > **别名精确 60** > tag 40 > 别名模糊 26 > tag 模糊 22 >
  标题 20 > 摘要 8 > 正文 3×n。`find`/`show` 输出别名。实测：标题「条件全清单」
  用英文提 `conditions list` 原基本淹没 → 加别名后 98 分排第一。
- **validate_kb.py**：新增 aliases 规范校验（必须 `[]` 形式、无重复、≤16 项；与 tags 重叠 → NOTE）。
  负向测试通过（重复项 / 20 项超限 / tags 重叠三条都触发）。

### 2) 新建 `01-navigation` 库（跨库排错索引）
- 一个条目 `troubleshooting-index.md`：「症状 → 条目路径 + **最易误判分叉点**」，
  按 动画姿势 / MO2 覆盖 / 画质 / CK 脚本 / 引擎补丁 五组组织。
- **关键设计**：它是**索引不是正文**——只给路径与分叉点，不复制正文，避免与六个库双份维护。
  分叉点比路径更值钱（如"OAR 的锅不会靠刷 Pandora 解决"）。
- 目录名**必须是 `01-navigation`**：`build_index.py` 只收 `NN-` 前缀目录，裸 `00-start` 会 0 条目。
  这个坑当场踩了一次，已写进 SKILL.md 硬约束。

### 3) `build_index.py` 根层规则修正（重要）
- **原行为**：`if rel == "." or top in IGNORE_DIRS: continue` —— 静默跳过**知识库根层**的所有 md。
  后果：条目写在库根 → 构建输出 `0 entries` **却不报任何错**。本次建 `01-navigation` 时踩到。
- **改法**：改成显式 `is_root = (rel == ".")`；根层只放 `manifest.json`/`index.*`/`README.md`；
  根层条目必须**自带 category 字段**，否则跳过。IGNORE 只看 `top`。
- **`validate_kb.py` 必须同步**：否则会出现「构建收入 1 条、校验数出 0 条」的假报错。
  新增 `ROOT_NON_ENTRY_FILES` 与 `declared_category_dirs()`，根层条目改为校验
  「category 是否在 manifest.categories 中声明」。
- **教训**：**构建器与校验器的收集规则必须逐条对齐**，否则校验器会变成噪音源。

### 4) `scripts/sync_scripts.py`（新增，防脚本分叉）
- 五件套脚本在技能 + 6 库共 7 份副本。按 sha256 比较，把不一致的库内副本用 stock 覆盖，
  覆盖前备份到 `_raw/pre-sync-<日期>/`。`--check` 模式只检查并返回非零退出码。
- **库发现规则**：工作区下任一含 `manifest.json` 的一级目录——**不按 `*-kb` 后缀判断**，
  否则 `01-navigation` 会被静默漏掉。
- 现状：7 份副本指纹各为 1，**无分叉**。

### 5) 入口层下沉技能
- `scripts/kb.py` + `scripts/sync_scripts.py` 已复制进技能 stock；
  新增 `references/agents-md-template.md`（AGENTS.md 模板 + 三条铁律 + 踩坑）。
- SKILL.md 新增章节：「让 agent 用得起这个库（多库工作区的入口层）」、
  「目录布局两条硬约束」、`aliases` 字段说明、kb.py / sync_scripts.py 脚本文档、
  工作流第 6 步（多库工作区）。

### 最终状态
- **6 库 / 198 条目**；`kb.py check` 通过；六库 validate/check_ui/check_links 全绿；
  7 份 stock 脚本指纹一致。
- 顺带修复了误跑技能脚本产生的残留（技能目录下的 index.json/html/`__pycache__` 已清理）。
- **Bash 坑复现**：`python -c` 里含 `^ ` 这类转义 + 多层嵌套引号时，命令会被静默吞掉
  （无输出、无报错、退出码 0）。可靠做法：**把脚本写成文件再执行**。

## 17:20 收尾：删除 `OAR/` 目录，清理全部悬挂引用

用户：「OAR目录已经去掉了，因为已经有了oar知识库」。

### 做了什么
- 确认根目录 `OAR/` 已不存在；现根层 = `01-navigation` + 5 个 `*-kb` + `AGENTS.md` + `scripts`
  + `.workbuddy` + `.git` + `.obsidian`。
- 全库 grep 出 **21 处**引用已删目录的文本，分布在 **15 个 md**。先用
  `kb.py grep` 核实被删内容的关键结论（BOM 检查、hkx 逐字节对比、`IsAttacking` 陷阱、
  六步改造法）**确已并入 `oar-kb`**，再改写。
- 临时脚本 `scripts/_fix_oar_refs.py`（用完即删）做**带断言的精确替换**
  （`assert text.count(old) == 1`）——防止静默替换到不该改的地方：
  - `` `OAR/OAR-补充文档.md` `` → `` `oar-kb/08-practices/`（原 `OAR/` 目录的内容已并入本库） ``
  - `` `OAR/oar改了动作要刷动作吗.md` `` → `` `oar-kb/00-overview/replacer-vs-patcher.md` ``
  - `` 本工作区实测记录 `OAR/` `` → `` 本工作区实测记录 `oar-kb/08-practices/` ``
  - `oar-dar.md` 与 `oar-kb/README.md` 各有一整行改写。
- `AGENTS.md:51` 路由行同步指向 `oar-kb/08-practices/`。
- **记忆文件也修了**（这是最后一块）：`MEMORY.md` 第 52–53 行原把
  `OAR\OAR-教程.md` / `OAR\OAR-补充文档.md` 当现存产物 —— 改成删除线 + 明确写「已并入
  `oar-kb`，不要再引用 `OAR\` 路径」；顺手修正三处过期计数（「第五个资料库」→「六个之一」、
  「五个资料库」→「六个」、「五库 25 份副本」→「六库 35 份」）。
  **技能路径也补正**：stock 版已从 `~/.workbuddy/skills/...` 迁到
  `D:\game\上古卷轴5\.workbuddy\skills\build-maintainable-kb\scripts\`。

### 校验（全绿）
- `kb.py check`：✓ 6 库 / 198 条目。
- 六库 `validate_kb.py`：ERRORS 0；`check_links.py`：链接全有效、全 LF。
- 六库 `check_index_ui.py`：`ALL_INDEX_UI_CHECKS_OK` ×6。
- `sync_scripts.py --check`：5 脚本指纹一致，无分叉。
- 残留检查：全库除「有意保留的前向指引」（`原 OAR/ 目录的内容已并入本库`、
  `AGENTS.md:51`）外，**无任何把 `OAR\` 当现存路径的引用**。

### 新教训：`check_index_ui.py` 是 Python 脚本，不是 Node 脚本
它内部才去调 Node 跑 DOM 桩。**用 `node check_index_ui.py` 会报
`SyntaxError: Invalid or unexpected token` 且退出码为 1**（因为它读到了
`# -*- coding: utf-8 -*-`）。正确调法：`<托管python> check_index_ui.py`。
**这条值得写进技能**——脚本名带 `.py` 却要用 Node 跑是反直觉的，而报错信息
（`SyntaxError`）会把人引向「文件编码坏了」的错误方向。

## 17:30–18:00 全库补齐 aliases（196 条）+ 修 kb.py 检索归一化

用户：「直接补吧」。

### 规模与做法
- 探针确认：198 条目中 **只有 2 条**已有 aliases（`conditions-list` + `01-navigation`），
  **待补 196 条**（分库：behaviour 28 / CS 64 / CK 29 / mo2 43 / oar 32）。
- 先导出全量 `id/title/tags/summary` 清单（`_dump_inventory.py`），据此**按内容**撰写别名，
  不是按文件名猜。
- 三个分片 JSON（part1/2/3）写好后合并审计，发现问题并修：
  1. **CS 库 47 条路径漏了子目录**（实际在 `02-features/core/` 与 `02-features/additional/`）。
  2. **~64 条别名与 tags 重复**（tags 已单独计 40 分，重复纯属浪费）→ 脚本自动剔除，共删 84 项。
  3. 剔除后有 10 条跌到 1~2 个别名 → 补写回 ≥3。
- 最终分布：**每条 3~7 个**（3:55 / 4:86 / 5:45 / 6:10 / 7:1），无超限、无空。

### 写回的安全设计（可复用）
用 `scripts/_apply_aliases.py`（用完即删）一次写回 196 个文件，四道保险：
① 锚点 = **唯一的 `tags:` 行**（写前探针确认 198/198 唯一）；
② 写前备份 `_raw/pre-aliases-2026-09-21/`（196 份，5 库）；
③ `newline=""` 读写防 CRLF 污染；
④ 逐个断言「frontmatter 仍闭合 + aliases 恰好 1 行 + tags 仍 1 行 + 无 `\r` + 正文逐字节未变」。
预演（`--dry`）确认 196 写 / 1 跳过 / 0 失败后才实跑。

### 顺手修了一个真 bug：`kb.py find` 匹配太死
带别名后实测发现**精确说法能命中，自然口语不命中**：
- `怎么装mod` 打不中别名 `怎么装 mod`（差一个空格）；
- `光源太多闪烁` 打不中别名 `光太多闪烁`（多两个字）。
根因：`terms = query.split()` —— **只按空白切词**，无词边界的所以中文整串当一个 term，
精确相等与"整串当子串"都失败。这不是别名写得不好，是**检索器对中文不友好**。

改了两处（`scripts/kb.py`）：
1. 新增 `_squeeze()`：比对前去掉所有空白。`怎么装mod` ≡ `怎么装 mod`。
2. 新增中文长串兜底：查询无空格且 ≥4 字时，拿库里**较短的别名反向包含**加分
   （alias~ 26 档 / alias≈ 14 档），既能召回又不会盖过精确命中。
另外新增 `_cjk_ngrams()` 备用（当前主路径已够用）。

**回归验证**：精确 id 分 126、`AttackState` 89、多词 `OAR priority` 130 —— 排名未被扰动；
此前 7 个自然口语查询里 5 个从不命中变成正确命中。
**诚实记录**：`拉弓一半跳`、`随便截图` 仍不命中——前者是 obito 拉弓的**本机经验、尚未成条目**
（只在 memory 里），后者是"随便"这种噪音词。**不是 bug，是库里确实没这条。**

### 校验（全绿）
六库 rebuild（198 条目全收）→ validate **ERRORS 0**（**零 aliases 相关告警**）→
check_links 全有效全 LF → check_ui `ALL_INDEX_UI_CHECKS_OK` ×6 → `sync_scripts.py --check` 无分叉
→ `kb.py check` ✓ 6 库/198 条。`index.json` 里 **带 aliases 的条目 = 198/198**。

### 同时写入
- `AGENTS.md` 的 aliases 段：补「空格不敏感」「中文长串反向包含」两条归一化 + "写短查询别写整句"。
- 技能 `SKILL.md`：新增「怎么写才真的有用」+「搜索端两条归一化」+ 批量回写脚本的三条硬要求。
